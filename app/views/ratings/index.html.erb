<h2>List of ratings</h2>

<p>Total of <%= pluralize(@ratings.count, 'rating') %></p>

<ul>
 <% @ratings.each do |rating| %>
   <li>
     <%= rating %>
     <%= link_to rating.user, user_path(rating.user) %>
    </li>
 <% end %> 
</ul>

<p><%= link_to 'New rating', new_rating_path %></p>